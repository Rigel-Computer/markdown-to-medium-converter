# docker-compose.yml - Claude Code mit OAuth (ohne API-Keys)
services:
   cc-mm-converter: # Sprechender Name für dein Projekt
      image: claude-code:latest
      container_name: container_cc-mm-converter # Auch hier sprechend
      volumes:
         - .:/workspace
         - ./claude-home:/home/node
      working_dir: /workspace
      stdin_open: true
      tty: true
      restart: unless-stopped
